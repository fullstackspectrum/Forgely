---
description: Suggest a git commit message for the current changes
---

Run `git diff HEAD` and `git status` to see what changed.

Output a single suggested commit message — no explanation, no preamble, no code block. Use the imperative mood, all lowercase, under 72 characters. If there are multiple distinct changes, add a short bullet-point body after a blank line.
